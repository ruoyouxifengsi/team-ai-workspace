ADMIN_USERNAME=admin
ADMIN_INITIAL_PASSWORD=change-me-immediately
SESSION_TTL_HOURS=168
CORS_ORIGINS=https://your-domain.example
DOMAIN=your-domain.example

# M2: DeepSeek + Fernet (required)
DEEPSEEK_API_KEY=sk-changeme
DEEPSEEK_BASE_URL=https://api.deepseek.com
# Generate: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# WARNING: once set, cannot be changed — all encrypted personal API keys become unreadable
FERNET_KEY=changeme-paste-fernet-generate-key-output-here
